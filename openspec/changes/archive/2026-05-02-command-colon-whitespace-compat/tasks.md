## Tasks

- [x] 盘点命令入口与链路
  - [x] 聊天：命令检测/提取的正则与分支
  - [x] 队列：定时器、关键字、enter/exit/return 的入队与消费
  - [x] 控制台/Agent 注入：入队判定逻辑

- [x] 设计并实现统一的命令规范化（空白容错）
  - [x] 在命令执行入口（`CommandManager.handle_message_commands`）应用规范化：支持行首空白、冒号后空白
  - [x] 在解析器（`CommandParser`）增加兜底 `lstrip()`，确保未来入口扩展不漏
  - [x] 处理空命令（`":"` / `":   "`）不触发执行

- [x] 补齐聊天入口：支持 `说：` 后空白 + 全角冒号
  - [x] 命令“存在性检测”正则升级：`说：\s*[:：]\s*\S...`
  - [x] 命令提取正则升级：`说：\s*[:：]\s*(.+)`
  - [x] 复用统一规范化逻辑，避免重复实现

- [x] 补齐控制台/Agent 注入入口：支持行首空白
  - [x] 将当前 `([:：].+)` 逻辑升级为允许 `^\s*[:：]\s*...`
  - [x] 仍通过队列消费，确保与其他入口一致

- [x] 验收用例矩阵（最少覆盖）
  - [x] 聊天：`说：:help` / `说： :help` / `说：：help` / `说： ： help`
  - [x] 队列（定时器/关键字/enter/exit/return）：`:help` / `: help` / `： help`
  - [x] 控制台/Agent：`: help` / `  : help` / `  ：  help`

- [x] 回归检查
  - [x] 原有无空格输入 `:help`、`:play xxx` 行为不变
  - [x] 非命令普通聊天不受影响（尤其 `说：` 后非冒号内容）


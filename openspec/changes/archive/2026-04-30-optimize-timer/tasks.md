## 1. 需求落点确认与定位

- [x] 1.1 在代码中定位 `timer add` 的命令实现（命令类/路由/解析函数）与 TimerManager 的创建入库入口
- [x] 1.2 确认 DB 模型/表对定时器 `id` 的唯一约束与当前冲突处理方式（是否会抛异常、是否已有重试）

## 2. `timer add`：ID 可选（自动生成）

- [x] 2.1 扩展参数解析：同时支持 `timer add <id> <时间> <消息>` 与 `timer add <时间> <消息>` 两种形态
- [x] 2.2 实现随机 ID 生成器（标准库，短且可读），并在入库冲突时重试（有限次数）
- [x] 2.3 更新成功回执：当省略 ID 时，回显最终生成的 ID（与落库一致）

## 3. `timer add`：纯数字时间 = 延迟秒数

- [x] 3.1 增加时间字段判定：当 `<时间>` 满足 `^[0-9]+$` 且 \(N>0\) 时，解释为 `now + N seconds`
- [x] 3.2 确保解析后的触发时间字段（例如 next_trigger）写入数据库与内存缓存，并在回执中展示可读的触发时间（用于自检）
- [x] 3.3 补充/调整错误提示：数字为 0、负数、或超大值时的提示与边界处理

## 4. 列表与管理一致性

- [x] 4.1 确保 `timer list` 展示最终 ID（无论用户提供或系统生成）
- [x] 4.2 确保 `timer remove <id>` 对系统生成 ID 同样可用（无额外前缀/格式差异）

## 5. 验证与回归

- [x] 5.1 增加一个最小可运行的脚本级测试（或复用现有测试模式），覆盖：省略 ID、数字延迟、冲突重试（可用 mock/临时插入）
- [x] 5.2 运行 `python -m py_compile` 对改动文件做语法检查，并运行 `pytest -q tests/test_timer_add.py` 做基础回归


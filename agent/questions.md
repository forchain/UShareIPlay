## 需要你补充信息的场景（最小问询模板）

### 新 UI / 新页面（anchors 不足）
- 目标页面的“成功锚点” element key 是什么？（建议 1-3 个）
- 失败时常见的弹窗/干扰锚点有哪些？
- 这些 element key 是否已写入 `config.yaml`？如果没有，请给出定位方式（id/xpath）与命名建议

### 新命令（断言口径不清）
- 命令的成功条件是什么？（发送了什么消息 / DB 变更 / 播放信息变化 / 其它）
- 成功后在日志/事件/DB 中应出现哪些可观测信号？

